<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CashKicker - Your finances</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; transition: background 0.3s, color 0.3s; }
    .font-display { font-family: 'Playfair Display', serif; }
    .accent-gradient { background: linear-gradient(135deg, #10B981, #34D399); }
    .waste-gradient { background: linear-gradient(135deg, #EF4444, #F87171); }
    .glass-card {
      background: rgba(30, 41, 59, 0.6);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    .glass-card-light {
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(0, 0, 0, 0.1);
    }
    .pop-in { animation: popIn 0.3s ease-out; }
    @keyframes popIn {
      0% { transform: scale(0.9); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    .fade-in { animation: fadeIn 0.2s ease-out; }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    .progress-fill { transition: width 0.5s ease; }
    .btn-hover { transition: all 0.2s ease; }
    .btn-hover:hover { transform: translateY(-1px); }
    .btn-hover:active { transform: translateY(0); }
    
    /* Light mode styles */
    body.light-mode {
      background: linear-gradient(to bottom, #f1f5f9, #e2e8f0) !important;
      color: #0f172a !important;
    }
    .light-mode .glass-card {
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(0, 0, 0, 0.1);
    }
    .light-mode input, .light-mode select {
      background: #f1f5f9 !important;
      border-color: #cbd5e1 !important;
      color: #0f172a !important;
    }
  </style>
</head>
<body class="min-h-screen text-slate-100 p-6 md:p-10" style="background: linear-gradient(to bottom, #0f172a, #1e293b);">

  <div class="max-w-md mx-auto">

    <!-- Settings Button -->
    <div class="fixed top-6 right-6 z-30">
      <button id="settings-btn" class="w-12 h-12 rounded-full accent-gradient flex items-center justify-center shadow-lg btn-hover">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
        </svg>
      </button>
    </div>

    <header class="mb-10 text-center">
      <div class="flex justify-center mb-6">
        <div class="w-20 h-20 rounded-full accent-gradient flex items-center justify-center shadow-2xl">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-10 h-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 10l7-7m0 0l7 7m-7-7v18" />
          </svg>
        </div>
      </div>
      
      <div class="flex items-center justify-center gap-2.5 mb-4">
        <div class="w-2.5 h-2.5 rounded-full accent-gradient"></div>
        <span class="text-xs uppercase tracking-widest font-semibold opacity-75">Your finances</span>
      </div>
      <h1 class="font-display text-6xl md:text-7xl font-black leading-tight" style="background: linear-gradient(135deg, #10B981, #34D399); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">CashKicker</h1>
    </header>

    <main id="main-content">
      <div id="goals-container" class="space-y-6"></div>
      
      <button id="create-goal-btn" class="w-full mt-8 py-4 rounded-2xl font-bold text-lg accent-gradient btn-hover shadow-lg">
        + Create New Goal
      </button>
    </main>

    <!-- Currency Selection (First Time) -->
    <div id="currency-setup" class="hidden fixed inset-0 bg-black bg-opacity-70 flex items-center justify-center p-6 z-50">
      <div class="glass-card rounded-3xl p-8 w-full max-w-md pop-in shadow-2xl">
        <h2 class="text-3xl font-bold mb-4 text-center">Welcome to CashKicker! 🎉</h2>
        <p class="text-center opacity-75 mb-6">Choose your currency to get started</p>
        
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Select Currency</label>
            <select id="currency-select" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-emerald-500 focus:outline-none">
              <option value="$">$ - US Dollar (USD)</option>
              <option value="€">€ - Euro (EUR)</option>
              <option value="£">£ - British Pound (GBP)</option>
              <option value="¥">¥ - Japanese Yen (JPY)</option>
              <option value="₹">₹ - Indian Rupee (INR)</option>
              <option value="₽">₽ - Russian Ruble (RUB)</option>
              <option value="R$">R$ - Brazilian Real (BRL)</option>
              <option value="C$">C$ - Canadian Dollar (CAD)</option>
              <option value="A$">A$ - Australian Dollar (AUD)</option>
              <option value="CHF">CHF - Swiss Franc (CHF)</option>
              <option value="¥">¥ - Chinese Yuan (CNY)</option>
              <option value="kr">kr - Swedish Krona (SEK)</option>
              <option value="kr">kr - Norwegian Krone (NOK)</option>
              <option value="₩">₩ - South Korean Won (KRW)</option>
              <option value="$">$ - Mexican Peso (MXN)</option>
              <option value="zł">zł - Polish Zloty (PLN)</option>
              <option value="R">R - South African Rand (ZAR)</option>
              <option value="$">$ - Singapore Dollar (SGD)</option>
              <option value="$">$ - Hong Kong Dollar (HKD)</option>
              <option value="﷼">﷼ - Saudi Riyal (SAR)</option>
              <option value="د.إ">د.إ - UAE Dirham (AED)</option>
              <option value="₺">₺ - Turkish Lira (TRY)</option>
            </select>
          </div>
        </div>
        
        <button id="save-currency-btn" class="w-full mt-6 py-3 rounded-xl accent-gradient btn-hover font-semibold shadow-lg">
          Let's Go! 🚀
        </button>
      </div>
    </div>

    <!-- Settings Modal -->
    <div id="settings-modal" class="hidden fixed inset-0 bg-black bg-opacity-70 flex items-center justify-center p-6 z-50 fade-in">
      <div class="glass-card rounded-3xl p-8 w-full max-w-md pop-in shadow-2xl">
        <h2 class="text-2xl font-bold mb-6">Settings</h2>
        
        <div class="space-y-6">
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Currency</label>
            <select id="settings-currency-select" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-emerald-500 focus:outline-none">
              <option value="$">$ - US Dollar (USD)</option>
              <option value="€">€ - Euro (EUR)</option>
              <option value="£">£ - British Pound (GBP)</option>
              <option value="¥">¥ - Japanese Yen (JPY)</option>
              <option value="₹">₹ - Indian Rupee (INR)</option>
              <option value="₽">₽ - Russian Ruble (RUB)</option>
              <option value="R$">R$ - Brazilian Real (BRL)</option>
              <option value="C$">C$ - Canadian Dollar (CAD)</option>
              <option value="A$">A$ - Australian Dollar (AUD)</option>
              <option value="CHF">CHF - Swiss Franc (CHF)</option>
              <option value="¥">¥ - Chinese Yuan (CNY)</option>
              <option value="kr">kr - Swedish Krona (SEK)</option>
              <option value="kr">kr - Norwegian Krone (NOK)</option>
              <option value="₩">₩ - South Korean Won (KRW)</option>
              <option value="$">$ - Mexican Peso (MXN)</option>
              <option value="zł">zł - Polish Zloty (PLN)</option>
              <option value="R">R - South African Rand (ZAR)</option>
              <option value="$">$ - Singapore Dollar (SGD)</option>
              <option value="$">$ - Hong Kong Dollar (HKD)</option>
              <option value="﷼">﷼ - Saudi Riyal (SAR)</option>
              <option value="د.إ">د.إ - UAE Dirham (AED)</option>
              <option value="₺">₺ - Turkish Lira (TRY)</option>
            </select>
          </div>
          
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Theme</label>
            <div class="flex gap-3">
              <button id="dark-mode-btn" class="flex-1 py-3 rounded-xl bg-slate-700 hover:bg-slate-600 transition-all font-semibold">
                🌙 Dark
              </button>
              <button id="light-mode-btn" class="flex-1 py-3 rounded-xl bg-slate-700 hover:bg-slate-600 transition-all font-semibold">
                ☀️ Light
              </button>
            </div>
          </div>
        </div>
        
        <button id="close-settings-btn" class="w-full mt-8 py-3 rounded-xl accent-gradient btn-hover font-semibold shadow-lg">
          Save & Close
        </button>
      </div>
    </div>

    <!-- Goal Creation Form -->
    <div id="goal-form" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-6 z-50 fade-in">
      <div class="glass-card rounded-3xl p-8 w-full max-w-md pop-in shadow-2xl">
        <h2 class="text-2xl font-bold mb-6">Create New Goal</h2>
        
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Goal Name</label>
            <input type="text" id="goal-name" placeholder="e.g., Emergency Fund" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-emerald-500 focus:outline-none transition-all">
          </div>
          
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Target Amount</label>
            <input type="number" id="goal-target" placeholder="1000" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-emerald-500 focus:outline-none transition-all">
          </div>
          
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Current Amount</label>
            <input type="number" id="goal-current" placeholder="0" value="0" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-emerald-500 focus:outline-none transition-all">
          </div>
        </div>
        
        <div class="flex gap-3 mt-8">
          <button id="cancel-goal-btn" class="flex-1 py-3 rounded-xl bg-slate-700 hover:bg-slate-600 transition-all font-semibold">
            Cancel
          </button>
          <button id="save-goal-btn" class="flex-1 py-3 rounded-xl accent-gradient btn-hover font-semibold shadow-lg">
            Save Goal
          </button>
        </div>
      </div>
    </div>

    <!-- Add Money Form -->
    <div id="add-money-form" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-6 z-50 fade-in">
      <div class="glass-card rounded-3xl p-8 w-full max-w-md pop-in shadow-2xl">
        <h2 class="text-2xl font-bold mb-6">Add Money</h2>
        
        <div class="mb-6">
          <p class="text-sm opacity-75 mb-2">Goal: <span id="current-goal-name" class="font-semibold text-emerald-400"></span></p>
          <p class="text-sm opacity-75">Current: <span id="current-goal-amount" class="font-semibold"></span></p>
        </div>
        
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Amount to Add</label>
            <input type="number" id="money-amount" placeholder="Enter amount" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-emerald-500 focus:outline-none transition-all">
          </div>
        </div>
        
        <div class="flex gap-3 mt-8">
          <button id="cancel-money-btn" class="flex-1 py-3 rounded-xl bg-slate-700 hover:bg-slate-600 transition-all font-semibold">
            Cancel
          </button>
          <button id="confirm-money-btn" class="flex-1 py-3 rounded-xl accent-gradient btn-hover font-semibold shadow-lg">
            Add Money
          </button>
        </div>
      </div>
    </div>

    <!-- Waste Money Form -->
    <div id="waste-money-form" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-6 z-50 fade-in">
      <div class="glass-card rounded-3xl p-8 w-full max-w-md pop-in shadow-2xl">
        <h2 class="text-2xl font-bold mb-6">Waste Money 💸</h2>
        
        <div class="mb-6">
          <p class="text-sm opacity-75 mb-2">Goal: <span id="waste-goal-name" class="font-semibold text-red-400"></span></p>
          <p class="text-sm opacity-75">Current: <span id="waste-goal-amount" class="font-semibold"></span></p>
        </div>
        
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold mb-2 opacity-75">Amount Wasted</label>
            <input type="number" id="waste-amount" placeholder="Enter amount" class="w-full px-4 py-3 rounded-xl bg-slate-700 border border-slate-600 focus:border-red-500 focus:outline-none transition-all">
          </div>
        </div>
        
        <div class="flex gap-3 mt-8">
          <button id="cancel-waste-btn" class="flex-1 py-3 rounded-xl bg-slate-700 hover:bg-slate-600 transition-all font-semibold">
            Cancel
          </button>
          <button id="confirm-waste-btn" class="flex-1 py-3 rounded-xl waste-gradient btn-hover font-semibold shadow-lg">
            Confirm Waste
          </button>
        </div>
      </div>
    </div>

    <!-- Celebration Modal -->
    <div id="celebration-modal" class="hidden fixed inset-0 bg-black bg-opacity-70 flex items-center justify-center p-6 z-50">
      <div class="text-center pop-in">
        <div class="text-8xl mb-6">🎉</div>
        <h2 class="text-5xl font-black mb-4 text-emerald-400">GOAL COMPLETED!</h2>
        <p id="completed-goal-name" class="text-2xl font-bold mb-2"></p>
        <p class="text-lg opacity-75">Amazing work! Keep it up! 💪</p>
      </div>
    </div>

  </div>

  <script>
    let goals = JSON.parse(localStorage.getItem('cashkicker-goals')) || [];
    let currency = localStorage.getItem('cashkicker-currency') || null;
    let theme = localStorage.getItem('cashkicker-theme') || 'dark';
    let currentGoalIndex = null;

    const goalsContainer = document.getElementById('goals-container');
    const createGoalBtn = document.getElementById('create-goal-btn');
    const goalForm = document.getElementById('goal-form');
    const cancelGoalBtn = document.getElementById('cancel-goal-btn');
    const saveGoalBtn = document.getElementById('save-goal-btn');
    
    const addMoneyForm = document.getElementById('add-money-form');
    const cancelMoneyBtn = document.getElementById('cancel-money-btn');
    const confirmMoneyBtn = document.getElementById('confirm-money-btn');
    const moneyAmountInput = document.getElementById('money-amount');
    
    const wasteMoneyForm = document.getElementById('waste-money-form');
    const cancelWasteBtn = document.getElementById('cancel-waste-btn');
    const confirmWasteBtn = document.getElementById('confirm-waste-btn');
    const wasteAmountInput = document.getElementById('waste-amount');
    
    const celebrationModal = document.getElementById('celebration-modal');
    
    const currencySetup = document.getElementById('currency-setup');
    const currencySelect = document.getElementById('currency-select');
    const saveCurrencyBtn = document.getElementById('save-currency-btn');
    
    const settingsBtn = document.getElementById('settings-btn');
    const settingsModal = document.getElementById('settings-modal');
    const settingsCurrencySelect = document.getElementById('settings-currency-select');
    const closeSettingsBtn = document.getElementById('close-settings-btn');
    const darkModeBtn = document.getElementById('dark-mode-btn');
    const lightModeBtn = document.getElementById('light-mode-btn');

    // Apply theme on load
    if (theme === 'light') {
      document.body.classList.add('light-mode');
    }

    // Check if first time user
    if (!currency) {
      currencySetup.classList.remove('hidden');
    }

    saveCurrencyBtn.addEventListener('click', () => {
      currency = currencySelect.value;
      localStorage.setItem('cashkicker-currency', currency);
      currencySetup.classList.add('hidden');
      renderGoals();
    });

    settingsBtn.addEventListener('click', () => {
      settingsCurrencySelect.value = currency;
      settingsModal.classList.remove('hidden');
    });

    closeSettingsBtn.addEventListener('click', () => {
      currency = settingsCurrencySelect.value;
      localStorage.setItem('cashkicker-currency', currency);
      settingsModal.classList.add('hidden');
      renderGoals();
    });

    darkModeBtn.addEventListener('click', () => {
      theme = 'dark';
      document.body.classList.remove('light-mode');
      localStorage.setItem('cashkicker-theme', 'dark');
    });

    lightModeBtn.addEventListener('click', () => {
      theme = 'light';
      document.body.classList.add('light-mode');
      localStorage.setItem('cashkicker-theme', 'light');
    });

    function celebrateGoal(goalName, goalIndex) {
      document.getElementById('completed-goal-name').textContent = goalName;
      celebrationModal.classList.remove('hidden');
      
      setTimeout(() => {
        celebrationModal.classList.add('hidden');
        goals.splice(goalIndex, 1);
        localStorage.setItem('cashkicker-goals', JSON.stringify(goals));
        renderGoals();
      }, 3000);
    }

    function renderGoals() {
      goalsContainer.innerHTML = '';
      
      if (goals.length === 0) {
        goalsContainer.innerHTML = `
          <div class="text-center py-12 opacity-50 fade-in">
            <p class="text-lg">No goals yet. Create your first one!</p>
          </div>
        `;
        return;
      }

      goals.forEach((goal, index) => {
        const progress = Math.min((goal.current / goal.target) * 100, 100);
        
        const goalCard = document.createElement('div');
        goalCard.className = 'glass-card rounded-3xl p-6 shadow-xl hover:shadow-2xl transition-all';
        goalCard.innerHTML = `
          <div class="flex justify-between items-start mb-4">
            <div>
              <h3 class="text-xl font-bold">${goal.name}</h3>
              <p class="text-sm opacity-75 mt-1">${currency}${goal.current.toLocaleString()} of ${currency}${goal.target.toLocaleString()}</p>
            </div>
            <button onclick="deleteGoal(${index})" class="text-red-400 hover:text-red-300 text-sm font-semibold transition-all">Delete</button>
          </div>
          
          <div class="relative h-3 bg-slate-700 rounded-full overflow-hidden mb-4">
            <div class="absolute inset-y-0 left-0 accent-gradient rounded-full progress-fill" style="width: ${progress}%"></div>
          </div>
          
          <div class="flex justify-between items-center gap-3">
            <span class="text-2xl font-bold text-emerald-400">${progress.toFixed(1)}%</span>
            <div class="flex gap-2">
              <button onclick="openWasteMoney(${index})" class="px-4 py-2 waste-gradient btn-hover rounded-lg font-semibold shadow-lg text-sm">
                💸 Waste
              </button>
              <button onclick="openAddMoney(${index})" class="px-4 py-2 accent-gradient btn-hover rounded-lg font-semibold shadow-lg text-sm">
                + Add
              </button>
            </div>
          </div>
        `;
        
        goalsContainer.appendChild(goalCard);
      });
    }

    function openAddMoney(index) {
      currentGoalIndex = index;
      document.getElementById('current-goal-name').textContent = goals[index].name;
      document.getElementById('current-goal-amount').textContent = `${currency}${goals[index].current.toLocaleString()}`;
      moneyAmountInput.value = '';
      addMoneyForm.classList.remove('hidden');
    }

    function openWasteMoney(index) {
      currentGoalIndex = index;
      document.getElementById('waste-goal-name').textContent = goals[index].name;
      document.getElementById('waste-goal-amount').textContent = `${currency}${goals[index].current.toLocaleString()}`;
      wasteAmountInput.value = '';
      wasteMoneyForm.classList.remove('hidden');
    }

    function deleteGoal(index) {
      if (confirm('Are you sure you want to delete this goal?')) {
        goals.splice(index, 1);
        localStorage.setItem('cashkicker-goals', JSON.stringify(goals));
        renderGoals();
      }
    }

    createGoalBtn.addEventListener('click', () => {
      goalForm.classList.remove('hidden');
    });

    cancelGoalBtn.addEventListener('click', () => {
      goalForm.classList.add('hidden');
    });

    saveGoalBtn.addEventListener('click', () => {
      const name = document.getElementById('goal-name').value;
      const target = parseFloat(document.getElementById('goal-target').value);
      const current = parseFloat(document.getElementById('goal-current').value) || 0;

      if (!name || !target || target <= 0) {
        alert('Please fill in all fields correctly');
        return;
      }

      goals.push({ name, target, current });
      localStorage.setItem('cashkicker-goals', JSON.stringify(goals));
      
      document.getElementById('goal-name').value = '';
      document.getElementById('goal-target').value = '';
      document.getElementById('goal-current').value = '0';
      
      goalForm.classList.add('hidden');
      renderGoals();
    });

    cancelMoneyBtn.addEventListener('click', () => {
      addMoneyForm.classList.add('hidden');
      currentGoalIndex = null;
    });

    confirmMoneyBtn.addEventListener('click', () => {
      const amount = parseFloat(moneyAmountInput.value);
      
      if (!amount || amount <= 0) {
        alert('Please enter a valid amount');
        return;
      }

      goals[currentGoalIndex].current += amount;
      
      if (goals[currentGoalIndex].current >= goals[currentGoalIndex].target) {
        localStorage.setItem('cashkicker-goals', JSON.stringify(goals));
        addMoneyForm.classList.add('hidden');
        celebrateGoal(goals[currentGoalIndex].name, currentGoalIndex);
        currentGoalIndex = null;
        return;
      }
      
      localStorage.setItem('cashkicker-goals', JSON.stringify(goals));
      addMoneyForm.classList.add('hidden');
      currentGoalIndex = null;
      renderGoals();
    });

    cancelWasteBtn.addEventListener('click', () => {
      wasteMoneyForm.classList.add('hidden');
      currentGoalIndex = null;
    });

    confirmWasteBtn.addEventListener('click', () => {
      const amount = parseFloat(wasteAmountInput.value);
      
      if (!amount || amount <= 0) {
        alert('Please enter a valid amount');
        return;
      }

      const newAmount = goals[currentGoalIndex].current - amount;
      goals[currentGoalIndex].current = Math.max(newAmount, 0);
      
      localStorage.setItem('cashkicker-goals', JSON.stringify(goals));
      wasteMoneyForm.classList.add('hidden');
      currentGoalIndex = null;
      renderGoals();
    });

    renderGoals();
  </script>
</body>
</html>
Here's the complete code with the Waste Money button included and minimal animations! Just copy and save this as an .html file. 🚀




